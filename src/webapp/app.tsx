import React from "react";
import ReactDOM from "react-dom";
ReactDOM.render(<App />, document.getElementById("root"));

export function App({}) {
  return <div className="text-gray-700 bg-blend-color">App!</div>;
}
